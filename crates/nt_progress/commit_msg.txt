feat(factory): implement ModeFactory for dependency injection

- Create ModeFactory struct to replace static REGISTRY
  - Add factory creation with default modes
  - Implement mode creation through factory
  - Add registry access method

- Add Debug trait to ModeCreator and implementations
  - Add Debug derive to ModeCreator trait
  - Add Debug derive to all creator structs
  - Add Debug derive to ModeRegistry

- Keep existing static registry for backward compatibility
  - Will be removed in future commits
  - Ensures no breaking changes

All tests passing with no regressions. 